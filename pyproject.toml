[project]
name = "forhacker-plugin-email-forensics"
version = "0.1.0"
description = "email-forensics Cell plugin for ForHacker"
requires-python = ">=3.12"
dependencies = ["forhacker"]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=4.0",
]
